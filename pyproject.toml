[project]
name = "documentchunkingmetricsframework"
version = "0.1.0"
description = "Document Chunking Metrics Framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
]
